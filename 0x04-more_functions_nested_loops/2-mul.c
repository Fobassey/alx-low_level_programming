#include "main.h"
/* betty style doc for function main goes there */
/**
 * mul-this multiplies two integers
 * @a: this are the integers
 * @b: this are the integers
 * Return: returns the equivalent values
 */

int mul(int a, int b)

{
	return (a * b);
}
