#include "main.h"
/**
 * _isupper - returns value for uppercase alphabet
 * @i: takes an integer value
 * Return: Always zero
 */

int _isupper(int i)

{
		if (i >= 65 && i <= 90)
		{
			return (1);
		}
		else
		{
			return (0);
		}
}
